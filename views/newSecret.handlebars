<h3 class="caveat">New Secret</h3>
    <div class="row red lighten-4 center">
      <div class="col s6 offset-s3">
        <div class="card-panel">
          <form class="form new-secret-form">
            <div class="form-group">
              <label for="secret-title">Title:</label>
              <input class="form-input" type="text" id="secret-title" name="secret-title" />
            </div>
            <div class="form-group">
              <label for="secret-body">What's your secret?</label>
              <textarea class="form-input" id="secret-body" name="secret-body"></textarea>
            </div>
            <div class="form-group">
              <button type="submit" class="btn btn-primary">Create</button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <script src="./js/add-secret.js"></script>
